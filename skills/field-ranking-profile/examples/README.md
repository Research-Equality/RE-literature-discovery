# field-ranking-profile examples

Inspect the bundled CS profile:

```bash
python skills/field-ranking-profile/scripts/get_profile.py --profile cs
```

Inspect the bundled biomedical profile:

```bash
python skills/field-ranking-profile/scripts/get_profile.py --profile biomedicine
```
