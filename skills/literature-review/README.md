# literature-review

Theme-level synthesis over an already ranked corpus.

Recommended input is an authority-aware `paper_db.jsonl` with `selection_bucket`, `authority_score`, `evidence_score`, and caution flags. Use `core` papers to anchor themes, `supporting` papers to widen coverage, and `background` papers for historical context.
